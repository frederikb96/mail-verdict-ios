#if DEBUG

    /// The composer's own screenshot entries — empty until S3 replaces the stub with the real
    /// editor and adds one entry per state worth capturing (empty, reply).
    enum ComposerScreenshots {
        static let entries: [MVScreenshotEntry] = []
    }

#endif
