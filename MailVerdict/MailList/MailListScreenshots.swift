#if DEBUG

    /// The mail list's own screenshot entries — empty until S1 replaces the stub with the real
    /// screen and adds one entry per state worth capturing (normal, select mode, swipe revealed).
    enum MailListScreenshots {
        static let entries: [MVScreenshotEntry] = []
    }

#endif
