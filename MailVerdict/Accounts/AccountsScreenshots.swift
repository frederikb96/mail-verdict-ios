#if DEBUG

    /// Accounts' own screenshot entries — empty until S7 replaces the stubs with the real list
    /// and detail screens.
    enum AccountsScreenshots {
        static let entries: [MVScreenshotEntry] = []
    }

#endif
