#if DEBUG

    /// Settings' own screenshot entries — empty until S7 replaces the stubs with the real forms.
    enum SettingsScreenshots {
        static let entries: [MVScreenshotEntry] = []
    }

#endif
