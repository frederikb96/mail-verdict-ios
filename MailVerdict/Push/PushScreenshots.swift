#if DEBUG

    /// The push block's own screenshot entries — empty until that block replaces the
    /// notification-settings stub with the real registration screen.
    enum PushScreenshots {
        static let entries: [MVScreenshotEntry] = []
    }

#endif
