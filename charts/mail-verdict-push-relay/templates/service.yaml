apiVersion: v1
kind: Service
metadata:
  name: {{ include "mail-verdict-push-relay.fullname" . }}
  labels:
    {{- include "mail-verdict-push-relay.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "mail-verdict-push-relay.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
